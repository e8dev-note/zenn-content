# zenn-content

Zenn の記事。[Zenn CLI](https://zenn.dev/zenn/articles/zenn-cli-guide) で管理。

```
npx zenn preview   # http://localhost:8000
npx zenn new:article
```

- `articles/` … 記事
- `images/` … 記事から `/images/...` で参照する画像
